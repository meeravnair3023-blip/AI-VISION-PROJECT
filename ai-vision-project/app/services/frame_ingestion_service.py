# 🔹 Process incoming frame (for future live video support)
def process_frame(frame):
    # Currently just returns the frame as-is
    # In future you can:
    #    - Resize image
    #    - Remove blur
    #    - Extract key frames
    return frame